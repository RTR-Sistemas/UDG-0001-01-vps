/* ===========================================================================
   ARQUIVO NEUTRALIZADO EM 06/09/2026 — migracao duplicada

   Era uma copia do 20260906120000_pq_key_binding.sql com o MESMO carimbo
   de tempo. Duas migracoes com o mesmo timestamp aplicam em ordem indefinida.
   O conteudo bom foi consolidado no arquivo sem o sufixo -1.

   O conteudo original foi preservado em: 20260906120000_pq_key_binding.sql (mesma pasta)
   Este arquivo continua existindo para nao quebrar o historico de migracoes
   ja aplicadas no banco, mas nao executa mais nada.
   =========================================================================== */

do $$ begin
  raise notice 'pq_key_binding-1: arquivo neutralizado; a migracao valida e a sem sufixo -1.';
end $$;
