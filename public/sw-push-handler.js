/**
 * Arquivo legado mantido apenas para compatibilidade com versões antigas do app.
 * A lógica oficial de push agora vive somente em /src/sw.js.
 *
 * Mantemos este arquivo sem listeners para evitar notificações duplicadas em
 * instalações antigas que ainda o importem por cache até a próxima atualização.
 */
